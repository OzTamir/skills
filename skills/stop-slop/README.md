# stop-slop (vendored)

This skill is **vendored from [hardikpandya/stop-slop](https://github.com/hardikpandya/stop-slop)**
(MIT, see [LICENSE](./LICENSE)) — not original to this repo.

`SKILL.md` and the files under `references/` are kept in sync with upstream
automatically by the shared
[`sync-vendored-skills`](../../.github/workflows/sync-vendored-skills.yml) workflow
(driven by [`.github/vendored-skills.json`](../../.github/vendored-skills.json)),
which fetches the latest copies from upstream's default branch weekly and
commits any changes directly to `main`. **Don't hand-edit `SKILL.md` or the
`references/` files here** — your changes will be overwritten on the next sync.
Contribute fixes upstream instead.
